query UserStats($userId: ID!) {
  user(id: $userId) {
    id
    lifetimeChestCount
    lifetimePremiumChestCount
    chestOpens(orderBy: timestamp, orderDirection: desc) {
      timestamp
      isPremium
    }
  }
}
